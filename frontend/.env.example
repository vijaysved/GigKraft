# GigKraft frontend env template. Copy to .env; never commit real secrets.
# Vite exposes only VITE_-prefixed vars to the client bundle.

VITE_API_BASE_URL=http://localhost:8000

# Google OAuth — must match the client ID in backend/.env
# Same client ID from https://console.cloud.google.com/
VITE_GOOGLE_CLIENT_ID=

# Stripe pricing table (test mode — from Stripe Dashboard → Pricing tables)
VITE_STRIPE_TEST_PRICING_TABLE_ID=
VITE_STRIPE_TEST_PUBLISHABLE_KEY=

# Stripe pricing table (live mode — from Stripe Dashboard in Live mode)
VITE_STRIPE_LIVE_PRICING_TABLE_ID=
VITE_STRIPE_LIVE_PUBLISHABLE_KEY=
