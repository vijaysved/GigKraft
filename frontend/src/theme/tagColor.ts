/** Fixed color for every #tag pill/badge across the app — a tag is always a filter,
 * so it always renders blue regardless of the page's active brand theme. */
export const TAG_FILTER_COLOR = "#228BE6";
