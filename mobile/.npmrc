# openapi-typescript declares a typescript ^5.x peer; the Expo SDK 56
# template ships typescript 6. The combo works (codegen CLI only), so relax
# peer resolution for installs.
legacy-peer-deps=true
